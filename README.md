# DevTiner Frontend

- created a vite + react project
- removed unnecessary code & print "Hello World"
- install tailwind css
- install daisyUI
- add navbar component to App.jsx
- add react-router-dom package
- create BrowserRouter
- Create parent-child routes and <Outlet />
- create a footer
- create a login page
- install axios
- CORS - install cors in backend => add middleware to app with configurations: origin, credentials
- whenever you are making an API call so pass axios => {withCredentials: true}
- Install redux toolkit - https://redux-toolkit.js.org/introduction/getting-started
- configure store => provider in app.js => create slice and export => add reducer to appStore
- show profile image after login
- navigate after login
- refactor our code with constants and created components folder
